#!/usr/bin/env groovy

def repoCredentials = ''
def repoURL = 'https://github.com/matmajk/python-web-db-project.git'

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying..'
            }
        }
    }
}